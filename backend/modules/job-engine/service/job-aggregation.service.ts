export class JobAggregationService {
  async fetchJobsFromAPI(): Promise<any[]> {
    // Mock external APIs for now
    return [];
  }

  async parseExternalJobs(rawJobs: any[]): Promise<any[]> {
    // Prepare for future
    return rawJobs;
  }
}
